# Replit (free tier, no card): auto-installs requirements.txt,
# seeds via Shell once, then serves the public webview URL.
run = "gunicorn app:app --bind 0.0.0.0:${PORT:-3000} --workers 1 --timeout 120"
